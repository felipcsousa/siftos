# Evidence

Supporting material for decisions may live in this directory.

A decision file itself records inline evidence with provenance:

    - Claim: <claim> | Source: <source> | Date: <YYYY-MM-DD>

`Source: unspecified` is a valid value. Never invent a source.

## Preserved records

- `DEC-0001-concierge-adoption.md` — session artifact from 2026-08-16:
  the "Community + LinkedIn launch" bet shaped, validated and challenged
  during a working session on a stale clone of this repository. It is
  preserved verbatim as evidence only; the live `DEC-0001` in
  `decisions/` is `DEC-0001-deterministic-core-first.md` (accepted). The
  session bet was never a live record here, so its ID was not reused.
