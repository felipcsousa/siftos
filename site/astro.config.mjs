import { defineConfig } from 'astro/config';

// GitHub Pages project site: https://felipcsousa.github.io/siftos/
export default defineConfig({
  site: 'https://felipcsousa.github.io',
  base: '/siftos/',
});